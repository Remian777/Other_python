# import time
# my_list = [1, 2, 3, 4, 5]
# total = len(my_list)
# for i, item in enumerate(my_list):
#     # 计算当前进度百分比
#     progress = (i + 1) / total * 100
#     print(f"Processing item {i+1}/{total} ({progress:.1f}%)")
#     # 模拟处理时间
#     time.sleep(0.5)
